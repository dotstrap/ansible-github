---

github_packages:
  - "{{ git_lfs | default(omit) }}"
  - "{{ git_extras | default(omit) }}"
  - "{{ hub | default(omit)  }}"
  - "{{ joe | default(omit)  }}"

install_state: present
# configuration_state: present

username: null
email: null
